import faker from 'faker';
import Profile from '../../model/profile';
import { removeAllResources } from './profile-mock';

const creatStudentMockPromise = async () => {
  const mockData = {};

  const mockStudent = {
    
  }
}
